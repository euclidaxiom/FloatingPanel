import SwiftUI
import FloatingPanel

import SwiftUI
import FloatingPanel

struct ContentView: View {
    @Environment(\.panelController) private var panelController

    var body: some View {
        EmptyView()
    }
}
